import { EventHandler } from "react"

export const Login = () => {
    
    const onSubmit: EventHandler
     = (event) => {

    }

    return (
        <form onSubmit={onSubmit}>

        </form>
    )
}